spring:
  application:
    nema: mall-gateway
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848

#动态刷新配置 ---需要忽略权限拦截
management:
  endpoints:
    web:
      exposure:
        include: refresh