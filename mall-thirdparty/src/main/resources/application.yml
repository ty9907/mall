server:
  port: 11000

spring:
  application:
    name: mall-third-party
  cloud:
    alicloud:
      oss:
        sts:
          access-key:
          secret-key:
        endpoint:
        bucket
      access-key:
      secret-key:
